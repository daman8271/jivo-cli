---
title: 2026-07-28 — SAP back up; July turnover delivered; attachment share blocked only by offline jivo node
created: 2026-07-28
updated: 2026-07-28
project: jivo-cli
type: chat
tags: [chats, sap, outage-recovery, turnover, attachments]
---

# 2026-07-28

Back-link: [[Chats-MOC]]

## SAP is BACK
- `sapb1 doctor` all green: `103.89.45.192:50000` reachable, login to `JIVO_OIL_HANADB` OK. The [[2026-07-27]] outage (whole app-server host down) is over on the Service Layer side.

## Delivered: Oil turnover this quarter (blocked since the 27th)
- **₹26.18 Cr net of GST** for 1–28 Jul 2026 (Oil / `JIVO_OIL_HANADB`).
- How: 479 `Invoices` (net `DocTotal−VatSum` = ₹35.92 Cr) − 116 `CreditNotes` (net ₹9.74 Cr), `Cancelled eq 'tNO'`, by `DocDate`.

## Goal #87 (the 2 UHBVN electricity-bill PDFs) — one flap away
- Creds are now IN HAND: `connections/fleet-access.env` has the JIVO-APP file-server login (`superadmin`) — the last blocker from [[2026-07-25]] is solved on paper.
- BUT the door is currently shut: tailnet node `jivo` (100.104.229.5, the subnet router advertising `20.20.45.0/24`) shows **offline, last seen ~2h** — tailscale ping times out, `20.20.45.25` unreachable (ping loss, 445 closed). Likely Tailscale on the Windows app server didn't (re)start cleanly after the outage, even though the SAP Service Layer host is serving again.
- Armed a session monitor polling `20.20.45.25:445`; the moment the route returns, pull `1721833122 (3).pdf` + `Jivo Wellness Mail - Electricity Bill of Factory APRIL 2026.pdf` from `\\20.20.45.25\Attachments_Oil\JIVO_OIL\Attachments`.
- If it doesn't come back on its own: someone at the office should check the SAP app server ("jivo" box) and start the Tailscale service (set it to auto-start while there).

## BREAKTHROUGH — direct read-only SQL into the SAP HANA database
- Owner supplied a **HANA DB login** (user `ZIA` = Ziyaul, accounts) on `103.89.45.192:30015`. Port 30015 = HANA SQL — the *actual SAP books*, not the limited Service Layer.
- Connected with a pure-Go driver (`SAP/go-hdb`, no native deps, works on Apple Silicon). Plaintext auth succeeds; all three company schemas visible: `JIVO_OIL_HANADB`, `JIVO_MART_HANADB`, `JIVO_BEVERAGES_HANADB`.
- **Why it matters:** real SQL — `SUM`/`GROUP BY`/`JOIN`. Turnover that used to mean downloading ~600 rows through the Service Layer is now **one query, one number, instant**. Kills the "no server-side aggregation" and "no `toupper` name search" limitations at a stroke.
- Built + saved a proper tool: **`hana-sql/`** (top-level, tracked). Strictly read-only by construction (first-token allowlist SELECT/WITH/EXPLAIN + single-statement guard — verified it REFUSES `UPDATE`). Reads creds from `connections/hana.env` (**gitignored**). Example query `queries/turnover-oil-july.sql`.
- **Live check (Oil, 1–28 Jul 2026, net of GST):** 495 invoices net ₹37.17 Cr − 122 credit notes net ₹9.74 Cr = **₹27.43 Cr turnover.** Slightly above the ₹26.18 Cr computed earlier this session via the Service Layer (479/116 rows) — HANA is the live DB and is authoritative; the earlier figure was an earlier snapshot. Can reconcile document-by-document if needed.
- **Hardening TODO (flagged to owner):** replace the personal `ZIA` login with a dedicated **read-only HANA service user** (SELECT-only grants on the 3 schemas) so read-only is DB-enforced, not just client-side, and the audit log shows a service account. Then optionally an MCP wrapper (like `postsql mcp`) for the Accounts team's Claude Desktop.

### Note: writes ambition is real, so lock the reads down first
Owner wants to start *writing* to SAP soon (most SAP work is data entry). Safe path already scoped in [[2026-07-28]] earlier notes: writes go through **Drafts** (47,485 already exist) with a human approving in the SAP client; named per-person SAP users; separate binary; dry-run + confirm; entity whitelist. `hana-sql` stays read-only forever — writes, if built, are a different tool over the Service Layer, not raw HANA.
