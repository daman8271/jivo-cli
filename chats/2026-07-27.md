# 2026-07-27

## SAP outage — server itself is down (both paths dead)
- Asked for: Oil turnover this quarter (1–27 Jul 2026, net of GST). **Blocked — SAP unreachable.**
- Evidence (differential, per the [[2026-07-25]] playbook):
  - Public endpoint `103.89.45.192:50000` — TCP dead from Mac Air AND from VPS.
  - Tailnet path — the `jivo` subnet-router node (`100.104.229.5`, the SAP app server itself) shows **offline, last seen ~1 day** in `tailscale status`; `20.20.45.0/24` route dead (ping 20.20.45.25 fails).
  - Control: postsql `103.89.45.76:5432` **answers fine** from the Mac Air → office server edge is UP.
- Conclusion: not a firewall/whitelist issue this time — with the box's own Tailscale agent offline too, the **SAP app server is down** (power/crash/network on the host). IT needs to check the machine.
- Side notes: Mac Air's tailscale SSH to `vps` wanted re-auth (fleet fell back to public IP fine); `macpro` unreachable on all routes (also shows offline on tailnet, last seen ~1h).
