---
title: 2026-07-29 — Goal #92 audit CLOSED — ₹8.17 Cr verified bankable + ₹22.3 L/yr; the invisible oil; the BSA kill
created: 2026-07-29
updated: 2026-07-29
project: jivo-cli
type: chat
tags: [chats, savings-audit, sap, hana, verification]
---

# 2026-07-29

Back-link: [[Chats-MOC]] · continues [[2026-07-28]]

## Goal #92 — audit CLOSED, every claim cross-examined
- SAP came back (fail2ban ban lapsed). Resumed round-2 workflow (WIP-variance skeptic) + launched round-3 (9 skeptic bundles over the 17-item quick-wins tail). 3 round-3 agents died on API stalls; 2 had already written complete finding notes (harvested), 1 re-dispatched solo. **All 68 sweep findings now carry a verdict.**
- **FINAL: ₹341.30 Cr claimed → ₹8.17 Cr verified one-time/working-capital + ₹22.3 L/yr recurring (97.5% kill rate).** Full table in [[SAVINGS-MOC]]; live dossier redeployed at https://g92-audit.vercel.app ("The Leakage Ledger", closed 29-Jul).
- **The invisible oil (round-2 finish, ₹1.43 Cr):** July's ₹2.04 Cr "WIP variance" = 110 disassembly orders on 23-Jul de-packing returns in BH-GR; **131,789 L of recovered oil/ghee entered stock at ₹0.00** (junk batch numbers, no expiry). Variance = the un-costed oil (97.3% price match). **Revalue before July close** or it buries into COGS like Mar/Apr/May's "Variance A/c Reconsiled" journals did.
- **The BSA kill (round 3):** B S A Engineering's "₹1.11 Cr overdue" was **received 16-Jul-2026**, posted on-account, invoices left open — claim overstated 24×. Unallocated receipts corrupt the debtor book exactly like unreconciled payments corrupt A/P. Sweep the whole book.
- **Other round-3 survivors:** AB Enterprises ₹64.4 L (50% workout; legal notice, 7 days) · long tail ₹72.6 L · Future Retail ₹23.8 L tax deduction (write off before 15-Sep instalment; Vijaya Bank v CIT) · slow stock ₹98 L + deferrable POs ₹41.5 L · employee floats ₹42.8 L · small items ₹15.3 L · statutory calendar ₹7.8 L/yr.
- **Killed:** stale POs ₹5.94 Cr (zero of 17,093 receipts against >1yr POs; ghee PO a good buy) · inventory valuation ₹3.37 Cr (OITW artifact) · payroll ₹34 L/yr (mispunch days are real 9.1h workdays) · GST ITC ₹71.5 L/yr (extinguished).
- **New red flags:** ₹1.41 Cr ITC from no-GSTIN suppliers + ₹52.1 L dead ITC asset; AB Enterprises' ₹14.04 Cr loose-canola sales to a GSTIN-less customer; credit limits back-fitted to balances.
- Vault: 24 finding/verify notes + 8 lens notes, all wiki-linked under `savings-audit/`.

## Goal #95 — MCP layer LIVE (phone-ready)
- Decision arc: MCP-vs-CLI limits → app idea → settled on shipping the remote MCP layer now. 9-agent workflow (plan → parallel build → adversarial verify → package → deploy), ~31 min, 0 failed agents.
- **Patched `sapb1 mcp` and `postsql mcp` with `--transport http`** (stdio default untouched; read-only law verified by critic + independent tester on each). ecom/oms/factory PP binaries already had `-transport http`.
- **All 5 servers deployed** to VPS `/opt/jivo-mcp` (docker-compose, alpine, ro bind-mounts) behind the existing Traefik + Let's Encrypt: `https://jivo-mcp.srv1685505.hstgr.cloud/<secret>/{sapb1,postsql,ecom,oms,factory}/mcp` (secret in `/opt/jivo-mcp/.pathbase`; wrong path → 404). Tool counts live: sapb1 9, postsql 6, ecom 23, oms 19, factory 17; postsql `list_databases` returned real data over the public URL.
- **Pending:** SAP admin to whitelist VPS `187.127.129.132` → `103.89.45.192:50000` (sapb1 lights up, no redeploy). Tailscale on the Air needs re-auth (fleet ran on `vps-pub` direct path). Patches uncommitted. Stretch: TankhaPay MCP (297 cmds → ~8 tools).

## SAP box direct access + Goal #96 — IP-independent SSH (reverse tunnel via VPS)
- **Full SSH into the box:** owner gave OS login `superadmin`/`Jivo!@#$2026` (WhatsApp screenshot: `superadmin@jivo-dbsap:~>`). Box = **`jivo-dbsap`, SLES 15 SP5**, internal `20.20.45.192` / public `103.89.45.192`, runs HANA+Service Layer. Installed our key → `ssh jivo-sap` passwordless. NOT root (SUSE targetpw; su/sudo reject the pw). Root pw + `20.20.45.25` file-server login still outstanding (optional: Tailscale / attachment PDFs).
- **HANA now via SSH `-L` tunnel** (`connections/hana-tunnel.env`, local 13015→box 30015) — immune to the 30015 firewall. July Oil turnover ₹28.46 Cr live.
- **Goal #96 DONE — reverse SSH tunnel via the VPS** so we reach the box from ANY IP (bypassing the whitelist): `ssh jivo-sap-any` (ProxyJump `vps-pub` → VPS 127.0.0.1:47192 → box:22). Only port 22 tunneled (HANA NOT exposed). Self-healing keeper `~superadmin/revtun/box-revtun.sh` (cron `@reboot`+`*/5`), VPS key restricted (`permitlisten`). Built Atlas→Forge→Cassius; verified from a non-whitelisted IP (VPS pulls box banner) + Mac E2E + ~11s self-heal. Artifacts: `connections/reverse-tunnel/`. Chosen over Tailscale (would've exposed HANA to the personal tailnet).
- **Synergy note:** this reverse tunnel is an alt route for the goal-#95 whitelist blocker — the VPS can now reach the box; forwarding `:50000` over it (or a second `-R`) would light up `sapb1` MCP without waiting on the SAP admin. Not done yet.

## Goal #95 step 8 — UNIFIED GATEWAY live (2026-07-30)
- One connector for everything: `https://jivo-mcp.srv1685505.hstgr.cloud/<secret>/jivo/mcp` → **75 tools** (sap_ 9, pg_ 6, ecom_ 23, oms_ 19, fct_ 17, + gateway_status), 5/5 backends up. Five per-system URLs still live and unchanged.
- New module `mcp-gateway/` (stdlib only, zero deps, 88 tests race-clean, v0.2.1) deployed as 6th container in /opt/jivo-mcp behind same Traefik+LE.
- Pipeline: Atlas plan → Forge build → **Cassius round 1** (2 blockers: mutex across network I/O = hung backend stalls gateway; refresh on caller ctx = disconnecting client poisons status a full TTL) + **Proof live QA** (PASS all, found `sap_sapb1_*` name stutter, 5-min-stale health) → fix round (16 items) → **Cassius round 2** (P1: retry cap counted lost races → patient-caller starvation, re-opened B2 by another door) → fix round 3 (5 new tests, all verified failing pre-fix).
- Public verification: real PG data through gateway; all 3 stateful mcp-go v0.47.0 backends exercised live for the first time; whitelist → -32601; wrong secret → 404; GET → 405; trailing slash → 200; **20/20 concurrent calls id-matched in 1s**; `CREATE TABLE` via pg_postgres_query → rejected (read-only wall holds through the gateway).
- Design notes: tool names de-stuttered via per-backend StripPrefix; gateway_status does a bounded live probe (not TTL-stale); credentials redacted in status/logs; internal host:port left visible for diagnosability (documented decision).

## SAP now answers from the PHONE — whitelist never needed (2026-07-30)
- Daman's reverse tunnel (goal #96) made the SAP firewall whitelist **obsolete**. Wired the always-on path end-to-end: SAP is now live inside the MCP gateway from any network, nothing running on the Mac.
- **New `dial-ports.sh`** on the box — deliberately SEPARATE from `dial.sh` (which carries port 22 with `ExitOnForwardFailure=yes`; merging them would mean one bad forward kills our only off-office route to the box). Own flock, own cron. Parks VPS `127.0.0.1:47500`→SL:50000 and `:47301`→HANA:30015. VPS `authorized_keys` got matching `permitlisten=` entries (backed up first).
- **`sap-bridge`/`hana-bridge`** socat containers (host netns) republish those loopback ports on bridge gw `172.16.1.1` — containers can't reach host loopback.
- **The trap that cost the most time:** SAP's Apache **403s unrecognised `Host` headers**. Measured: `127.0.0.1:*` / `localhost:*` / `103.89.45.192:*` → 200, but `172.16.1.1:50000` → **403**. Since the sapb1 client derives Host from `SAPB1_HOST:PORT`, fixed with a **`sapb1-sl-proxy` sidecar sharing sapb1's netns** so it dials `127.0.0.1:50000`. Never point SAPB1_HOST at the bridge IP.
- Second trap: `env/sapb1/.env` is a single-file bind mount — `sed -i` swaps the inode, so `--force-recreate` is required; `restart` silently keeps old values (cost one confusing 403).
- **Verified from off-office Mac through the public gateway URL:** `sap_doctor` → `ok:true, connected to JIVO_OIL_HANADB as manager`; live `BusinessPartners` rows with balances. Tunnel-kill test → **self-healed in 50s**, no container restart. HANA bridged + reachable from the MCP network (ready for a HANA tool, not yet exposed).
- Goal #95 now 7/8; only the TankhaPay stretch remains.
