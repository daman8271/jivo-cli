---
title: 2026-07-30 — SAP fully reachable from HOME (data + 105 GB attachments); access documented
created: 2026-07-30
updated: 2026-07-30
project: jivo-cli
type: chat
tags: [chats, sap, hana, attachments, home-access, reverse-tunnel]
---

# 2026-07-30

Back-link: [[Chats-MOC]] · continues [[2026-07-29]]

## Proved: 100% of SAP reachable from home, off-office (non-whitelisted home IP, ~01:00 IST)
Daman at home (Jivo), not office. Probed **every** route live. Result: everything works through the goal-#96 reverse tunnel (`ssh jivo-sap-any`); direct-to-box is dead from home by design (office-IP whitelist).
- **HANA data — 100%.** Tunnel `-L 13015:127.0.0.1:30015 jivo-sap-any` → `hana-sql -env connections/hana-tunnel.env`. Verified real books: 29,851 Oil A/R invoices, latest `DocDate` = 29-Jul (current). All 3 schemas visible.
- **SAP B1 Service Layer — 100%.** Tunnel `-L 15000:…:50000` → `SAPB1_HOST=localhost SAPB1_PORT=15000 ./sapb1 doctor` → "All checks passed" (login as `manager`).
- **The SAP box is a jump host to the whole JIVO LAN** — from it, 445/5985 (Windows), 5432 (postsql), 5001/9080/1433 (JSAP/control-panel/DSR), 445 (file-server) all OPEN. One `-L` each away.

## The confusion, cleared: DATA vs FILES
HANA (the data, all numbers) runs **only on the Linux box** `jivo-dbsap` (103.89.45.192) — every DB port on the Windows box is closed. The **Windows box** `JIVO-APP` (20.20.45.25) holds only the scanned **attachment files**, not data. SSH'ing Windows would give zero data. Filing-cabinet (Linux) vs photocopy-room (Windows).

## Attachments: full admin, measured, pulled home
Daman re-shared the Windows login (`ADMIN`) — turned out it was **already saved** in `connections/fleet-access.env` (`JIVOAPP_SMB_*`) and matched exactly (my earlier "not stored" was a grep miss on the filename; corrected). `smbclient` runs **on the Linux box** (reaches 20.20.45.25:445), password fed via SSH stdin.
- Shares: `Attachments_{Oil,Mart,Bev}`, `OMS_Attachments`, `"Jivo Oil/Mart/Beverages"` (B1_SHR), `C$/D$/E$` (full admin).
- **Measured (recurse ON; ls, summed):** Oil 180,693 files / 65.2 GB · Mart 75,487 / 16.1 GB · Bev 49,908 / 23.3 GB · OMS 0 (empty/recheck) → **~306k files / ~104.6 GB.** More than the ATC1 DB registry (~202k) — folders also hold WordDocs/XMLDocs/screenshots.
- **End-to-end proof:** pulled `TAN.pdf` (166 KB, valid PDF) from the share → box → this Mac at 23 MB/s.
- Alt route: public RDP `103.89.45.25:13579`.

## Saved properly
- Runbook: **`connections/SAP-HOME-ACCESS.md`** (all routes, cheat sheet, no secrets) + linked from [[CONNECTIONS_MOC]].
- Memory: new `sap-home-access.md`; updated `sap-data-sizing-and-mirror.md` (measured attachments + home-access confirmed) + `sap-hana-direct-sql.md` pointer + MEMORY.md index.
- Hardening: consolidated secrets onto `fleet-access.env`, deleted the duplicate I'd made, `chmod 600` on all `connections/*.env` (were 644), confirmed all gitignored.

## Still open / next
- **Reachability is solved; WAN-dependency isn't** — the box lives in the office, so if the office WAN dies, so does the tunnel. Durable fix = the approved **HANA→Postgres mirror on the VPS** (books ≈4.6 GB, trivial); attachments = phase 2 (~105 GB SMB sync).
- `vision/` folder created for tomorrow's intent (not yet filled).
