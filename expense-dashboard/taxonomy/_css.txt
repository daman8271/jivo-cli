
:root{
 --bg:#0b0f14; --panel:#111823; --panel2:#0e141d; --line:#1e2a38; --line2:#2a3a4d;
 --ink:#e8eef6; --ink2:#9fb0c4; --ink3:#6b7f96;
 --accent:#4ade80; --accent2:#22d3ee; --warn:#fbbf24; --bad:#f87171;
 --oil:#4ade80; --mart:#38bdf8; --bev:#c084fc;
 --mono:ui-monospace,SFMono-Regular,"SF Mono",Menlo,Consolas,monospace;
 --sans:-apple-system,BlinkMacSystemFont,"Segoe UI",Inter,Roboto,Helvetica,Arial,sans-serif;
}
*{box-sizing:border-box}
html,body{margin:0;padding:0}
body{background:var(--bg);color:var(--ink);font-family:var(--sans);font-size:15px;line-height:1.55;-webkit-font-smoothing:antialiased}
a{color:var(--accent2)}
.wrap{max-width:1360px;margin:0 auto;padding:0 22px 120px}
header.hero>.wrap,nav.toc>.wrap{padding-bottom:0}
header.hero{border-bottom:1px solid var(--line);background:linear-gradient(180deg,#0d141d 0%,var(--bg) 100%);padding:40px 0 28px;margin-bottom:0}
.eyebrow{font-family:var(--mono);font-size:11px;letter-spacing:.18em;text-transform:uppercase;color:var(--accent);margin:0 0 10px}
h1{font-size:clamp(28px,4vw,44px);line-height:1.1;margin:0 0 12px;letter-spacing:-.02em}
h1 small{display:block;font-size:clamp(14px,1.6vw,18px);font-weight:400;color:var(--ink2);letter-spacing:0;margin-top:8px}
.stamp{font-family:var(--mono);font-size:12px;color:var(--ink3);margin-top:14px}
.stamp b{color:var(--ink2);font-weight:600}
nav.toc{position:sticky;top:0;z-index:50;background:rgba(11,15,20,.92);backdrop-filter:blur(10px);border-bottom:1px solid var(--line);margin:0 -22px 0;padding:0 22px}
nav.toc ul{display:flex;gap:2px;list-style:none;margin:0;padding:0;overflow-x:auto;scrollbar-width:thin}
nav.toc a{display:block;white-space:nowrap;padding:12px 12px;font-size:12.5px;color:var(--ink2);text-decoration:none;border-bottom:2px solid transparent;font-family:var(--mono)}
nav.toc a:hover{color:var(--ink);border-bottom-color:var(--line2)}
section{padding:44px 0 8px;scroll-margin-top:52px}
h2{font-size:22px;margin:0 0 6px;letter-spacing:-.01em}
h2 .num{font-family:var(--mono);font-size:13px;color:var(--accent);margin-right:10px}
.sub{color:var(--ink2);font-size:13.5px;margin:0 0 20px;max-width:88ch}
.panel{background:var(--panel);border:1px solid var(--line);border-radius:12px;padding:18px 20px;margin:14px 0}
.panel.tight{padding:0;overflow:hidden}
.kpis{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(210px,100%),1fr));gap:12px;margin:18px 0}
.kpi{background:var(--panel);border:1px solid var(--line);border-radius:12px;padding:16px 18px;min-width:0}
.kpi .lab{font-family:var(--mono);font-size:10.5px;letter-spacing:.12em;text-transform:uppercase;color:var(--ink3);margin-bottom:8px}
.kpi .val{font-size:26px;font-weight:650;letter-spacing:-.02em;font-variant-numeric:tabular-nums;line-height:1.15;overflow-wrap:anywhere}
.kpi .sub2{font-size:12px;color:var(--ink2);margin-top:6px;font-variant-numeric:tabular-nums}
.kpi.oil{border-left:3px solid var(--oil)} .kpi.mart{border-left:3px solid var(--mart)}
.kpi.bev{border-left:3px solid var(--bev)} .kpi.grp{border-left:3px solid var(--warn)}
.tblwrap{overflow-x:auto;border:1px solid var(--line);border-radius:12px;background:var(--panel)}
table{border-collapse:collapse;width:100%;font-size:13.5px;min-width:640px}
th,td{padding:9px 12px;text-align:right;border-bottom:1px solid var(--line);white-space:nowrap}
th{background:var(--panel2);font-family:var(--mono);font-size:10.5px;letter-spacing:.1em;text-transform:uppercase;color:var(--ink3);font-weight:600;position:sticky;top:0}
td:first-child,th:first-child{text-align:left;white-space:normal;min-width:170px}
tbody tr:hover{background:#141d29}
td.num{font-variant-numeric:tabular-nums;font-family:var(--mono);font-size:12.5px}
tr.tot td{font-weight:700;background:#0d1620;border-top:2px solid var(--line2)}
tr.sec td{background:#0d1620;font-family:var(--mono);font-size:11px;letter-spacing:.1em;text-transform:uppercase;color:var(--ink3)}
.neg{color:var(--bad)} .pos{color:var(--ink)}
.muted{color:var(--ink3)}
.chip{display:inline-block;font-family:var(--mono);font-size:10.5px;padding:2px 7px;border-radius:5px;border:1px solid var(--line2);color:var(--ink2);margin-right:5px}
.chip.oil{color:var(--oil);border-color:#1c4b32} .chip.mart{color:var(--mart);border-color:#14415c}
.chip.bev{color:var(--bev);border-color:#432c5e} .chip.x{color:var(--warn);border-color:#4a3a12}
.controls{display:flex;flex-wrap:wrap;gap:10px;align-items:center;margin:16px 0;padding:14px 16px;background:var(--panel2);border:1px solid var(--line);border-radius:12px}
.controls .lab{font-family:var(--mono);font-size:10.5px;letter-spacing:.12em;text-transform:uppercase;color:var(--ink3);margin-right:2px}
button.tg{background:#0b1119;border:1px solid var(--line2);color:var(--ink2);font-family:var(--mono);font-size:12px;padding:6px 12px;border-radius:7px;cursor:pointer}
button.tg:hover{border-color:var(--accent);color:var(--ink)}
button.tg[aria-pressed=true]{background:#12301f;border-color:var(--accent);color:var(--accent)}
input.search{background:#0b1119;border:1px solid var(--line2);color:var(--ink);font-family:var(--mono);font-size:12.5px;padding:7px 11px;border-radius:7px;min-width:220px;flex:1}
input.search::placeholder{color:var(--ink3)}
.callout{border-left:3px solid var(--warn);background:#1a1508;border-radius:0 10px 10px 0;padding:14px 18px;margin:16px 0;font-size:13.5px}
.callout.bad{border-left-color:var(--bad);background:#1c0e0e}
.callout.ok{border-left-color:var(--accent);background:#0c1a12}
.callout h4{margin:0 0 6px;font-size:13.5px;font-family:var(--mono);letter-spacing:.06em;text-transform:uppercase;color:var(--warn)}
.callout.bad h4{color:var(--bad)} .callout.ok h4{color:var(--accent)}
.callout p{margin:6px 0 0;color:var(--ink2)}
.chart{width:100%;overflow-x:auto}
svg{display:block}
.legend{display:flex;gap:16px;flex-wrap:wrap;font-family:var(--mono);font-size:11.5px;color:var(--ink2);margin-top:12px}
.legend i{display:inline-block;width:10px;height:10px;border-radius:2px;margin-right:6px;vertical-align:middle}
details.drill{border-top:1px solid var(--line)}
details.drill>summary{cursor:pointer;padding:10px 14px;font-size:13px;list-style:none;display:flex;justify-content:space-between;gap:14px;align-items:center}
details.drill>summary::-webkit-details-marker{display:none}
details.drill>summary:hover{background:#141d29}
details.drill>summary .cc{font-weight:600}
details.drill>summary .amt{font-family:var(--mono);font-size:12.5px;font-variant-numeric:tabular-nums;color:var(--ink2)}
details.drill[open]>summary{background:#0d1620;border-bottom:1px solid var(--line)}
.grid2{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(320px,100%),1fr));gap:14px}
dl.defs{margin:0}
dl.defs dt{font-family:var(--mono);font-size:12px;color:var(--accent);margin-top:12px}
dl.defs dd{margin:3px 0 0;color:var(--ink2);font-size:13.5px}
code{font-family:var(--mono);font-size:12.5px;background:#0b1119;border:1px solid var(--line);padding:1px 5px;border-radius:4px;color:var(--ink2)}
pre{background:#0b1119;border:1px solid var(--line);border-radius:10px;padding:14px;overflow-x:auto;font-family:var(--mono);font-size:11.5px;color:var(--ink2);line-height:1.5}
footer{border-top:1px solid var(--line);margin-top:60px;padding-top:22px;color:var(--ink3);font-size:12px;font-family:var(--mono)}
.bar-row{display:grid;grid-template-columns:150px 1fr 110px;gap:12px;align-items:center;padding:6px 0;font-size:13px}
.bar-track{background:#0b1119;border-radius:4px;height:20px;position:relative;overflow:hidden;border:1px solid var(--line)}
.bar-fill{height:100%;border-radius:3px}
.bar-val{font-family:var(--mono);font-size:12px;text-align:right;font-variant-numeric:tabular-nums}
@media(max-width:700px){.bar-row{grid-template-columns:110px 1fr 90px;font-size:12px}}
