# Flow — `OBTF` (Journal Voucher (parked batch)) · MART

Lines from `BTF1`, last 365 days. The copy-from link is what decides how much of an entry is typed and how much arrives for free.

_`BTF1` carries no `BaseType` — this document is never copied from anything._
