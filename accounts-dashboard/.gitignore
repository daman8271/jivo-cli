# The built data snapshot is JIVO's live debtor and creditor book — party names
# and balances. The Vercel deployment is public by the owner's decision; git
# history is a separate and permanent one, so the data stays out of the repo.
# Rebuild it any time with: python3 pipeline/build_data.py
site/data.json
site/data.json.tmp
pipeline/raw/
pipeline/refresh.log
pipeline/refresh.launchd.log
pipeline/__pycache__/
site/.vercel/
