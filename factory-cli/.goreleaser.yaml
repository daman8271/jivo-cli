version: 2
project_name: jivo-factory-pp-cli
changelog:
  disable: true
builds:
  - id: jivo-factory-pp-cli
    main: ./cmd/jivo-factory-pp-cli
    binary: jivo-factory-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X jivo-factory-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: jivo-factory-pp-mcp
    main: ./cmd/jivo-factory-pp-mcp
    binary: jivo-factory-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: jivo-factory-pp-cli
    repository:
      owner: daman8271
      name: homebrew-tap
    homepage: "https://github.com/daman8271/jivo-factory-pp-cli"
    description: "JIVO factory management CLI (ji.jivo.in / factory.jivo."
    install: |
      bin.install "jivo-factory-pp-cli"
      bin.install "jivo-factory-pp-mcp"
