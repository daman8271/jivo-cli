// Copyright 2026 daman8271 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newDispatchPlansDispatchFulfilmentBillsCmd(flags *rootFlags) *cobra.Command {
	var flagFrom string
	var flagTo string
	var flagStatus string
	var flagSearch string
	var flagLimit int
	var flagOffset string

	cmd := &cobra.Command{
		Use:         "dispatch-fulfilment-bills",
		Short:       "Bill-by-bill fulfilment list — what was billed versus what actually went out the gate, with the fulfilment rate",
		Example:     "  jivo-factory-pp-cli dispatch-plans dispatch-fulfilment-bills",
		Annotations: map[string]string{"pp:endpoint": "dispatch-plans.dispatch-fulfilment-bills", "pp:method": "GET", "pp:path": "/dispatch-plans/dashboard/bills/", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/dispatch-plans/dashboard/bills/"
			params := map[string]string{}
			if flagFrom != "" {
				params["from"] = formatCLIParamValue(flagFrom)
			}
			if flagTo != "" {
				params["to"] = formatCLIParamValue(flagTo)
			}
			if flagStatus != "" {
				params["status"] = formatCLIParamValue(flagStatus)
			}
			if flagSearch != "" {
				params["search"] = formatCLIParamValue(flagSearch)
			}
			if flagLimit != 0 {
				params["limit"] = formatCLIParamValue(flagLimit)
			}
			if flagOffset != "" {
				params["offset"] = formatCLIParamValue(flagOffset)
			}
			data, prov, err := resolveReadWithStrategy(cmd.Context(), c, flags, "auto", "dispatch-plans", false, path, params, nil, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Honor --limit when the API accepts but ignores ?limit=N.
			data = truncateJSONArray(data, flagLimit)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagFrom, "from", "", "Optional server-side (a bare GET returned all 1921 bills). (YYYY-MM-DD)")
	cmd.Flags().StringVar(&flagTo, "to", "", "see from (YYYY-MM-DD)")
	cmd.Flags().StringVar(&flagStatus, "status", "", "PENDING | BOOKED | DISPATCHED | CANCELLED. The UI's 'All' tab sends an empty string / omits it.")
	cmd.Flags().StringVar(&flagSearch, "search", "", "UI placeholder 'Search invoice / customer…'. Verified live: search=626078398 gave count 1.")
	cmd.Flags().IntVar(&flagLimit, "limit", 0, "Default 50; the UI page size is 25. int")
	cmd.Flags().StringVar(&flagOffset, "offset", "", "Real offset pagination, unlike /dispatch-plans/bills/. int")

	return cmd
}
