// Copyright 2026 daman8271 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string `json:"command"`
	Description  string `json:"description"`
	Group        string `json:"group,omitempty"`
	WhyItMatters string `json:"why_it_matters,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "accounts departments", Description: "GET /accounts/departments/ — departments list (15 rows, shared across companies)", Group: "accounts"},
	{Command: "accounts me", Description: "GET /accounts/me/ — accounts me", Group: "accounts"},
	{Command: "accounts users", Description: "GET /accounts/users/ — accounts users", Group: "accounts"},
	{Command: "barcode boxes", Description: "GET /barcode/boxes/ — barcode boxes", Group: "barcode"},
	{Command: "barcode dispatch-reports", Description: "GET /barcode/dispatch/reports/ — barcode dispatch reports", Group: "barcode"},
	{Command: "barcode dispatch-reports-boxes", Description: "GET /barcode/dispatch/reports/boxes/ — barcode dispatch reports boxes", Group: "barcode"},
	{Command: "barcode dispatch-reports-pallets", Description: "GET /barcode/dispatch/reports/pallets/ — barcode dispatch reports pallets", Group: "barcode"},
	{Command: "barcode dispatch-reports-rejected-scans", Description: "GET /barcode/dispatch/reports/rejected-scans/ — barcode dispatch reports rejected scans", Group: "barcode"},
	{Command: "barcode dispatch-sessions", Description: "GET /barcode/dispatch/sessions/ — barcode dispatch sessions (all)", Group: "barcode"},
	{Command: "barcode dispatch-sessions-active", Description: "GET /barcode/dispatch/sessions/active/ — barcode dispatch sessions active", Group: "barcode"},
	{Command: "barcode dispatch-sessions-closed", Description: "GET /barcode/dispatch/sessions/closed/ — barcode dispatch sessions closed", Group: "barcode"},
	{Command: "barcode dispatch-sessions-completed", Description: "GET /barcode/dispatch/sessions/completed/ — barcode dispatch sessions completed", Group: "barcode"},
	{Command: "barcode dispatch-sessions-from-bill", Description: "GET /barcode/dispatch/sessions/from-bill/ — barcode dispatch sessions from bill", Group: "barcode"},
	{Command: "barcode dispatch-settings", Description: "GET /barcode/dispatch/settings/ — barcode dispatch settings", Group: "barcode"},
	{Command: "barcode intercompany-dashboard", Description: "GET /barcode/intercompany/dashboard/ — barcode intercompany dashboard", Group: "barcode"},
	{Command: "barcode intercompany-trace", Description: "GET /barcode/intercompany/trace/ — trace an intercompany barcode", Group: "barcode"},
	{Command: "barcode intercompany-transfers", Description: "GET /barcode/intercompany/transfers/ — barcode intercompany transfers list", Group: "barcode"},
	{Command: "barcode items-oitm", Description: "GET /barcode/items/oitm/ — barcode items oitm", Group: "barcode"},
	{Command: "barcode loose", Description: "GET /barcode/loose/ — barcode loose", Group: "barcode"},
	{Command: "barcode pallets", Description: "GET /barcode/pallets/ — barcode pallets", Group: "barcode"},
	{Command: "barcode print-history", Description: "GET /barcode/print/history/ — barcode print history", Group: "barcode"},
	{Command: "barcode production-release-oil", Description: "GET /barcode/production-release-oil/ — oil production release (JIVO_OIL only; 503 for other companies)", Group: "barcode"},
	{Command: "barcode scan-history", Description: "GET /barcode/scan/history/ — barcode scan history", Group: "barcode"},
	{Command: "company companies", Description: "GET /company/companies/ — the 3 companies backing the Select-Company screen", Group: "company"},
	{Command: "daily-needs-gatein gate-entries-daily-need-categories", Description: "GET /daily-needs-gatein/gate-entries/daily-need/categories/ — daily needs gatein gate entries daily need categories", Group: "daily-needs-gatein"},
	{Command: "dashboards inventory-age-filter-options", Description: "GET /dashboards/inventory-age/filter-options/ — dashboards inventory age filter options", Group: "dashboards"},
	{Command: "dashboards inventory-age-report", Description: "GET /dashboards/inventory-age/report/ — dashboards inventory age report", Group: "dashboards"},
	{Command: "dashboards sales-planning-requirement-analysis", Description: "GET /dashboards/sales-planning-requirement/analysis/ — dashboards sales planning requirement analysis", Group: "dashboards"},
	{Command: "dashboards sales-planning-requirement-report", Description: "GET /dashboards/sales-planning-requirement/report/ — dashboards sales planning requirement report", Group: "dashboards"},
	{Command: "dashboards sales-planning-requirement-status", Description: "GET /dashboards/sales-planning-requirement/status/ — dashboards sales planning requirement status", Group: "dashboards"},
	{Command: "dashboards stock", Description: "GET /dashboards/stock/ — dashboards stock", Group: "dashboards"},
	{Command: "dashboards stock-as-of", Description: "GET /dashboards/stock/as-of/ — stock snapshot as of a date", Group: "dashboards"},
	{Command: "dispatch bilty-grpo-history", Description: "GET /dispatch/bilty-grpo/history/ — dispatch bilty grpo history", Group: "dispatch"},
	{Command: "dispatch bilty-grpo-options", Description: "GET /dispatch/bilty-grpo/options/ — dispatch bilty grpo options", Group: "dispatch"},
	{Command: "dispatch bilty-grpo-pending", Description: "GET /dispatch/bilty-grpo/pending/ — dispatch bilty grpo pending", Group: "dispatch"},
	{Command: "dispatch bilty-grpo-preview", Description: "GET /dispatch/bilty-grpo/preview/{dispatch_plan_id}/ — bilty-GRPO posting preview for a dispatch plan", Group: "dispatch"},
	{Command: "dispatch open-bilties", Description: "GET /dispatch/open-bilties/ — dispatch open bilties", Group: "dispatch"},
	{Command: "dispatch transporter-invoices-history", Description: "GET /dispatch/transporter-invoices/history/ — dispatch transporter invoices history", Group: "dispatch"},
	{Command: "dispatch-plans bills", Description: "GET /dispatch-plans/bills/ — bills in a date window", Group: "dispatch-plans"},
	{Command: "dispatch-plans pipeline", Description: "GET /dispatch-plans/pipeline/ — dispatch plans pipeline", Group: "dispatch-plans"},
	{Command: "docking-admin partial-scan-requests", Description: "GET /docking-admin/partial-scan-requests/ — docking admin partial scan requests", Group: "docking-admin"},
	{Command: "docking-admin scan-skip-requests", Description: "GET /docking-admin/scan-skip-requests/ — docking admin scan skip requests", Group: "docking-admin"},
	{Command: "driver-management drivers", Description: "GET /driver-management/drivers/ — driver management drivers", Group: "driver-management"},
	{Command: "driver-management drivers-names", Description: "GET /driver-management/drivers/names/ — driver management drivers names", Group: "driver-management"},
	{Command: "gate-core arrivals", Description: "GET /gate-core/arrivals/ — gate core arrivals", Group: "gate-core"},
	{Command: "gate-core arrivals-expected", Description: "GET /gate-core/arrivals/expected/ — expected arrival for a vehicle", Group: "gate-core"},
	{Command: "gate-core bst-ins", Description: "GET /gate-core/bst-ins/ — gate core bst ins", Group: "gate-core"},
	{Command: "gate-core bst-ins-eligible-outs", Description: "GET /gate-core/bst-ins/eligible-outs/ — gate core bst ins eligible outs", Group: "gate-core"},
	{Command: "gate-core bst-outs", Description: "GET /gate-core/bst-outs/ — gate core bst outs", Group: "gate-core"},
	{Command: "gate-core bst-outs-sap-transfers", Description: "GET /gate-core/bst-outs/sap-transfers/ — gate core bst outs sap transfers", Group: "gate-core"},
	{Command: "gate-core bst-returns", Description: "GET /gate-core/bst-returns/ — gate core bst returns", Group: "gate-core"},
	{Command: "gate-core bst-returns-eligible-outs", Description: "GET /gate-core/bst-returns/eligible-outs/ — gate core bst returns eligible outs", Group: "gate-core"},
	{Command: "gate-core empty-vehicle-ins", Description: "GET /gate-core/empty-vehicle-ins/ — gate core empty vehicle ins", Group: "gate-core"},
	{Command: "gate-core empty-vehicle-ins-eligible", Description: "GET /gate-core/empty-vehicle-ins/eligible/ — gate core empty vehicle ins eligible", Group: "gate-core"},
	{Command: "gate-core empty-vehicle-ins-reasons", Description: "GET /gate-core/empty-vehicle-ins/reasons/ — gate core empty vehicle ins reasons", Group: "gate-core"},
	{Command: "gate-core empty-vehicle-outs", Description: "GET /gate-core/empty-vehicle-outs/ — gate core empty vehicle outs", Group: "gate-core"},
	{Command: "gate-core empty-vehicle-outs-eligible-entries", Description: "GET /gate-core/empty-vehicle-outs/eligible-entries/ — gate core empty vehicle outs eligible entries", Group: "gate-core"},
	{Command: "gate-core job-work", Description: "GET /gate-core/job-work/ — gate core job work", Group: "gate-core"},
	{Command: "gate-core job-work-sap-grpos", Description: "GET /gate-core/job-work/sap-grpos/ — gate core job work sap grpos", Group: "gate-core"},
	{Command: "gate-core job-work-sap-production-orders", Description: "GET /gate-core/job-work/sap-production-orders/ — gate core job work sap production orders", Group: "gate-core"},
	{Command: "gate-core rejected-qc-returns", Description: "GET /gate-core/rejected-qc-returns/ — gate core rejected qc returns", Group: "gate-core"},
	{Command: "gate-core sales-dispatch", Description: "GET /gate-core/sales-dispatch/ — gate core sales dispatch", Group: "gate-core"},
	{Command: "gate-core sales-dispatch-documents", Description: "GET /gate-core/sales-dispatch/documents/ — gate core sales dispatch documents", Group: "gate-core"},
	{Command: "gate-core sales-dispatch-lock", Description: "GET /gate-core/sales-dispatch/lock/ — gate core sales dispatch lock", Group: "gate-core"},
	{Command: "gate-core sales-dispatch-pending-bookings", Description: "GET /gate-core/sales-dispatch/pending-bookings/ — gate core sales dispatch pending bookings", Group: "gate-core"},
	{Command: "gate-core sales-dispatch-reports", Description: "GET /gate-core/sales-dispatch/reports/ — gate core sales dispatch reports", Group: "gate-core"},
	{Command: "grpo all-entries", Description: "GET /grpo/all-entries/ — grpo all entries", Group: "grpo"},
	{Command: "grpo history", Description: "GET /grpo/history/ — grpo history", Group: "grpo"},
	{Command: "grpo history-detail", Description: "GET /grpo/history/{posting_id}/ — a posted material GRPO", Group: "grpo"},
	{Command: "grpo pending", Description: "GET /grpo/pending/ — grpo pending", Group: "grpo"},
	{Command: "grpo preview", Description: "GET /grpo/preview/{vehicle_entry_id}/ — GRPO preview for a vehicle entry", Group: "grpo"},
	{Command: "grpo service-history", Description: "GET /grpo/service/history/ — grpo service history", Group: "grpo"},
	{Command: "grpo service-history-detail", Description: "GET /grpo/service/history/{posting_id}/ — a posted service GRPO", Group: "grpo"},
	{Command: "grpo service-options", Description: "GET /grpo/service/options/ — grpo service options", Group: "grpo"},
	{Command: "grpo service-pending", Description: "GET /grpo/service/pending/ — grpo service pending", Group: "grpo"},
	{Command: "grpo service-preview", Description: "GET /grpo/service/preview/{dispatch_plan_id}/ — service-GRPO preview for a dispatch plan", Group: "grpo"},
	{Command: "grpo summary", Description: "GET /grpo/summary/ — grpo summary", Group: "grpo"},
	{Command: "maintenance alerts", Description: "GET /maintenance/alerts/ — maintenance alerts", Group: "maintenance"},
	{Command: "maintenance asset-categories", Description: "GET /maintenance/asset-categories/ — maintenance asset categories", Group: "maintenance"},
	{Command: "maintenance asset-departments", Description: "GET /maintenance/asset-departments/ — maintenance asset departments", Group: "maintenance"},
	{Command: "maintenance asset-documents", Description: "GET /maintenance/asset-documents/ — maintenance asset documents", Group: "maintenance"},
	{Command: "maintenance asset-locations", Description: "GET /maintenance/asset-locations/ — maintenance asset locations", Group: "maintenance"},
	{Command: "maintenance asset-photos", Description: "GET /maintenance/asset-photos/ — maintenance asset photos", Group: "maintenance"},
	{Command: "maintenance assets", Description: "GET /maintenance/assets/ — maintenance assets", Group: "maintenance"},
	{Command: "maintenance dashboard", Description: "GET /maintenance/dashboard/ — maintenance dashboard", Group: "maintenance"},
	{Command: "maintenance options", Description: "GET /maintenance/options/ — maintenance options", Group: "maintenance"},
	{Command: "maintenance pm-checklist-items", Description: "GET /maintenance/pm-checklist-items/ — maintenance pm checklist items", Group: "maintenance"},
	{Command: "maintenance pm-executions", Description: "GET /maintenance/pm-executions/ — maintenance pm executions", Group: "maintenance"},
	{Command: "maintenance pm-plans", Description: "GET /maintenance/pm-plans/ — maintenance pm plans", Group: "maintenance"},
	{Command: "maintenance reports", Description: "GET /maintenance/reports/ — maintenance reports", Group: "maintenance"},
	{Command: "maintenance scan-lookup", Description: "GET /maintenance/scan/lookup/ — asset/spare scan lookup", Group: "maintenance"},
	{Command: "maintenance spare-categories", Description: "GET /maintenance/spare-categories/ — maintenance spare categories", Group: "maintenance"},
	{Command: "maintenance spare-movements", Description: "GET /maintenance/spare-movements/ — maintenance spare movements", Group: "maintenance"},
	{Command: "maintenance spare-requests", Description: "GET /maintenance/spare-requests/ — maintenance spare requests", Group: "maintenance"},
	{Command: "maintenance spares", Description: "GET /maintenance/spares/ — maintenance spares", Group: "maintenance"},
	{Command: "maintenance spares-low-stock", Description: "GET /maintenance/spares/low-stock/ — maintenance spares low stock", Group: "maintenance"},
	{Command: "maintenance spares-stock", Description: "GET /maintenance/spares/stock/ — spare stock lookup by code", Group: "maintenance"},
	{Command: "maintenance vendor-visits", Description: "GET /maintenance/vendor-visits/ — maintenance vendor visits", Group: "maintenance"},
	{Command: "maintenance work-order-photos", Description: "GET /maintenance/work-order-photos/ — maintenance work order photos", Group: "maintenance"},
	{Command: "maintenance work-orders", Description: "GET /maintenance/work-orders/ — maintenance work orders", Group: "maintenance"},
	{Command: "non-moving-rm item-groups", Description: "GET /non-moving-rm/item-groups/ — non moving rm item groups", Group: "non-moving-rm"},
	{Command: "non-moving-rm report", Description: "GET /non-moving-rm/report/ — non-moving raw-material report (replaces the dead bare endpoint)", Group: "non-moving-rm"},
	{Command: "notifications list", Description: "GET /notifications/ — notifications", Group: "notifications"},
	{Command: "notifications preferences", Description: "GET /notifications/preferences/ — notifications preferences", Group: "notifications"},
	{Command: "notifications unread-count", Description: "GET /notifications/unread-count/ — notifications unread count", Group: "notifications"},
	{Command: "person-gatein contractors", Description: "GET /person-gatein/contractors/ — contractor master", Group: "person-gatein"},
	{Command: "person-gatein entries", Description: "GET /person-gatein/entries/ — person gate-in entries (paginated)", Group: "person-gatein"},
	{Command: "person-gatein gates", Description: "GET /person-gatein/gates/ — gate master", Group: "person-gatein"},
	{Command: "person-gatein labours", Description: "GET /person-gatein/labours/ — labour master", Group: "person-gatein"},
	{Command: "person-gatein person-types", Description: "GET /person-gatein/person-types/ — person type master", Group: "person-gatein"},
	{Command: "person-gatein visitors", Description: "GET /person-gatein/visitors/ — visitor master", Group: "person-gatein"},
	{Command: "po vendors", Description: "GET /po/vendors/ — po vendors", Group: "po"},
	{Command: "po warehouses", Description: "GET /po/warehouses/ — po warehouses", Group: "po"},
	{Command: "product resolve", Description: "Resolve an exact price-scraper code, listing identity, JID, or company-qualified Factory item through the shared read-only product map", Group: "product-identity"},
	{Command: "product search", Description: "Search product identity display text without using names as operational join keys", Group: "product-identity"},
	{Command: "product catalog", Description: "List qualified Factory products and their price-listing mapping disposition for one explicit company", Group: "product-identity"},
	{Command: "product verify", Description: "Verify the shared product identity map is released, evidence-backed, complete, and safe to consume", Group: "product-identity"},
	{Command: "product coverage", Description: "Show zero-gap price SKU, listing, JID, and qualified Factory item coverage", Group: "product-identity"},
	{Command: "production-execution breakdown-categories", Description: "GET /production-execution/breakdown-categories/ — production execution breakdown categories", Group: "production-execution"},
	{Command: "production-execution checklist-templates", Description: "GET /production-execution/checklist-templates/ — production execution checklist templates", Group: "production-execution"},
	{Command: "production-execution costs-analytics", Description: "GET /production-execution/costs/analytics/ — production execution costs analytics", Group: "production-execution"},
	{Command: "production-execution line-clearance", Description: "GET /production-execution/line-clearance/ — production execution line clearance", Group: "production-execution"},
	{Command: "production-execution line-configs", Description: "GET /production-execution/line-configs/ — production execution line configs", Group: "production-execution"},
	{Command: "production-execution line-configs-auto-fill", Description: "GET /production-execution/line-configs/auto-fill/ — auto-fill config for a production line", Group: "production-execution"},
	{Command: "production-execution lines", Description: "GET /production-execution/lines/ — production execution lines", Group: "production-execution"},
	{Command: "production-execution machine-checklists", Description: "GET /production-execution/machine-checklists/ — production execution machine checklists", Group: "production-execution"},
	{Command: "production-execution machines", Description: "GET /production-execution/machines/ — production execution machines", Group: "production-execution"},
	{Command: "production-execution reports-analytics", Description: "GET /production-execution/reports/analytics/ — production execution reports analytics", Group: "production-execution"},
	{Command: "production-execution reports-analytics-cost-analysis", Description: "GET /production-execution/reports/analytics/cost-analysis/ — production execution reports analytics cost analysis", Group: "production-execution"},
	{Command: "production-execution reports-analytics-downtime", Description: "GET /production-execution/reports/analytics/downtime/ — production execution reports analytics downtime", Group: "production-execution"},
	{Command: "production-execution reports-analytics-downtime-pareto", Description: "GET /production-execution/reports/analytics/downtime-pareto/ — production execution reports analytics downtime pareto", Group: "production-execution"},
	{Command: "production-execution reports-analytics-monthly-summary", Description: "GET /production-execution/reports/analytics/monthly-summary/ — production execution reports analytics monthly summary", Group: "production-execution"},
	{Command: "production-execution reports-analytics-oee", Description: "GET /production-execution/reports/analytics/oee/ — production execution reports analytics oee", Group: "production-execution"},
	{Command: "production-execution reports-analytics-oee-trend", Description: "GET /production-execution/reports/analytics/oee-trend/ — production execution reports analytics oee trend", Group: "production-execution"},
	{Command: "production-execution reports-analytics-plan-vs-production", Description: "GET /production-execution/reports/analytics/plan-vs-production/ — production execution reports analytics plan vs production", Group: "production-execution"},
	{Command: "production-execution reports-analytics-procurement-vs-planned", Description: "GET /production-execution/reports/analytics/procurement-vs-planned/ — procurement fulfillment vs plan for a SAP order", Group: "production-execution"},
	{Command: "production-execution reports-analytics-resource-consumption", Description: "GET /production-execution/reports/analytics/resource-consumption/ — production execution reports analytics resource consumption", Group: "production-execution"},
	{Command: "production-execution reports-analytics-waste", Description: "GET /production-execution/reports/analytics/waste/ — production execution reports analytics waste", Group: "production-execution"},
	{Command: "production-execution reports-analytics-waste-trend", Description: "GET /production-execution/reports/analytics/waste-trend/ — production execution reports analytics waste trend", Group: "production-execution"},
	{Command: "production-execution reports-daily-production", Description: "GET /production-execution/reports/daily-production/ — daily production report", Group: "production-execution"},
	{Command: "production-execution reports-line-clearance", Description: "GET /production-execution/reports/line-clearance/ — production execution reports line clearance", Group: "production-execution"},
	{Command: "production-execution reports-production-movement", Description: "GET /production-execution/reports/production-movement/ — production execution reports production movement", Group: "production-execution"},
	{Command: "production-execution reports-production-movement-filter-options", Description: "GET /production-execution/reports/production-movement/filter-options/ — production execution reports production movement filter options", Group: "production-execution"},
	{Command: "production-execution runs", Description: "GET /production-execution/runs/ — production execution runs", Group: "production-execution"},
	{Command: "production-execution sap-bom", Description: "GET /production-execution/sap/bom/ — bill of materials for an item", Group: "production-execution"},
	{Command: "production-execution sap-items", Description: "GET /production-execution/sap/items/ — production execution sap items", Group: "production-execution"},
	{Command: "production-execution sap-orders", Description: "GET /production-execution/sap/orders/ — production execution sap orders", Group: "production-execution"},
	{Command: "production-execution waste", Description: "GET /production-execution/waste/ — production execution waste", Group: "production-execution"},
	{Command: "quality-control arrival-slips", Description: "GET /quality-control/arrival-slips/ — quality control arrival slips", Group: "quality-control"},
	{Command: "quality-control inspections", Description: "GET /quality-control/inspections/ — quality control inspections", Group: "quality-control"},
	{Command: "quality-control inspections-actionable", Description: "GET /quality-control/inspections/actionable/ — quality control inspections actionable", Group: "quality-control"},
	{Command: "quality-control inspections-awaiting-chemist", Description: "GET /quality-control/inspections/awaiting-chemist/ — quality control inspections awaiting chemist", Group: "quality-control"},
	{Command: "quality-control inspections-awaiting-qam", Description: "GET /quality-control/inspections/awaiting-qam/ — quality control inspections awaiting qam", Group: "quality-control"},
	{Command: "quality-control inspections-completed", Description: "GET /quality-control/inspections/completed/ — quality control inspections completed", Group: "quality-control"},
	{Command: "quality-control inspections-counts", Description: "GET /quality-control/inspections/counts/ — quality control inspections counts", Group: "quality-control"},
	{Command: "quality-control inspections-draft", Description: "GET /quality-control/inspections/draft/ — quality control inspections draft", Group: "quality-control"},
	{Command: "quality-control inspections-pending", Description: "GET /quality-control/inspections/pending/ — quality control inspections pending", Group: "quality-control"},
	{Command: "quality-control inspections-rejected", Description: "GET /quality-control/inspections/rejected/ — quality control inspections rejected", Group: "quality-control"},
	{Command: "quality-control inspections-return-to-vendor", Description: "GET /quality-control/inspections/return-to-vendor/ — quality control inspections return to vendor", Group: "quality-control"},
	{Command: "quality-control material-types", Description: "GET /quality-control/material-types/ — quality control material types", Group: "quality-control"},
	{Command: "quality-control print-documents", Description: "GET /quality-control/print-documents/ — quality control print documents", Group: "quality-control"},
	{Command: "quality-control production-qc", Description: "GET /quality-control/production-qc/ — quality control production qc", Group: "quality-control"},
	{Command: "quality-control production-qc-counts", Description: "GET /quality-control/production-qc/counts/ — quality control production qc counts", Group: "quality-control"},
	{Command: "quality-control production-qc-pending", Description: "GET /quality-control/production-qc/pending/ — quality control production qc pending", Group: "quality-control"},
	{Command: "quality-control sap-items", Description: "GET /quality-control/sap-items/ — quality control sap items", Group: "quality-control"},
	{Command: "sap plan-dashboard-details", Description: "GET /sap/plan-dashboard/details/ — sap plan dashboard details", Group: "sap"},
	{Command: "sap plan-dashboard-procurement", Description: "GET /sap/plan-dashboard/procurement/ — sap plan dashboard procurement", Group: "sap"},
	{Command: "sap plan-dashboard-summary", Description: "GET /sap/plan-dashboard/summary/ — sap plan dashboard summary", Group: "sap"},
	{Command: "vehicle-management transporters", Description: "GET /vehicle-management/transporters/ — vehicle management transporters", Group: "vehicle-management"},
	{Command: "vehicle-management transporters-names", Description: "GET /vehicle-management/transporters/names/ — vehicle management transporters names", Group: "vehicle-management"},
	{Command: "vehicle-management vehicle-entries", Description: "GET /vehicle-management/vehicle-entries/ — vehicle entries in a window", Group: "vehicle-management"},
	{Command: "vehicle-management vehicle-entries-count", Description: "GET /vehicle-management/vehicle-entries/count/ — vehicle entry counts in a window", Group: "vehicle-management"},
	{Command: "vehicle-management vehicle-entries-list-by-status", Description: "GET /vehicle-management/vehicle-entries/list-by-status/ — vehicle entries grouped by status", Group: "vehicle-management"},
	{Command: "vehicle-management vehicle-types", Description: "GET /vehicle-management/vehicle-types/ — vehicle management vehicle types", Group: "vehicle-management"},
	{Command: "vehicle-management vehicles", Description: "GET /vehicle-management/vehicles/ — vehicle management vehicles", Group: "vehicle-management"},
	{Command: "vehicle-management vehicles-names", Description: "GET /vehicle-management/vehicles/names/ — vehicle management vehicles names", Group: "vehicle-management"},
	{Command: "warehouse bom-requests", Description: "GET /warehouse/bom-requests/ — warehouse bom requests", Group: "warehouse"},
	{Command: "warehouse fg-receipts", Description: "GET /warehouse/fg-receipts/ — warehouse fg receipts", Group: "warehouse"},
	{Command: "warehouse wms-batches-expiry", Description: "GET /warehouse/wms/batches/expiry/ — warehouse wms batches expiry", Group: "warehouse"},
	{Command: "warehouse wms-billing-overview", Description: "GET /warehouse/wms/billing/overview/ — warehouse wms billing overview", Group: "warehouse"},
	{Command: "warehouse wms-dashboard", Description: "GET /warehouse/wms/dashboard/ — warehouse wms dashboard", Group: "warehouse"},
	{Command: "warehouse wms-item-groups", Description: "GET /warehouse/wms/item-groups/ — warehouse wms item groups", Group: "warehouse"},
	{Command: "warehouse wms-sales-orders-backlog", Description: "GET /warehouse/wms/sales-orders/backlog/ — warehouse wms sales orders backlog", Group: "warehouse"},
	{Command: "warehouse wms-stock-movements", Description: "GET /warehouse/wms/stock/movements/ — warehouse wms stock movements", Group: "warehouse"},
	{Command: "warehouse wms-stock-overview", Description: "GET /warehouse/wms/stock/overview/ — warehouse wms stock overview", Group: "warehouse"},
	{Command: "warehouse wms-transfers-overview", Description: "GET /warehouse/wms/transfers/overview/ — warehouse wms transfers overview", Group: "warehouse"},
	{Command: "warehouse wms-warehouses", Description: "GET /warehouse/wms/warehouses/ — warehouse wms warehouses", Group: "warehouse"},
	{Command: "warehouse wms-warehouses-summary", Description: "GET /warehouse/wms/warehouses/summary/ — warehouse wms warehouses summary", Group: "warehouse"},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := strings.Fields(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"mcp:read-only":       "true",
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  jivo-factory-pp-cli which "stale tickets"
  jivo-factory-pp-cli which "bottleneck"
  jivo-factory-pp-cli which --limit 1 "send message"
  jivo-factory-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
