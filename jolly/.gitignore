# The reason these were held back was that jivo-cli used to be a PUBLIC repo.
# It is PRIVATE now (verified 2026-09-02), and Gurvinderjeet Singh — who owns
# monthly planning — works from an iPad through a cloud Claude Code session and
# has no other way to receive them. Daman's call, 2026-09-02.
#
# people/  real names, WhatsApp numbers, reporting lines
# plan/    the monthly planning workbooks
# briefs/  the monthly brief workbooks
# out/     derived per-SKU plans, costs, margins (regenerable from engine/)
# sim/     the frozen simulator inputs

.DS_Store
node_modules/
.next/
.vercel/
*.tsbuildinfo

# Mark 3 live re-plan outputs — rewritten every 3 minutes by live/loop.sh (freeze -> sim).
# Same reason as live/state/: churn that would make the VPS's daily pull fight its own
# loop. Nothing is hidden — the publisher serves the results over HTTPS.
sim/live-inputs.json
sim/live-carry.json
sim/summary-live.json
sim/events-live.json
sim/days-live/
