# Shift board bars — 8 September 2026

Public deployment: `dpl_DUWWS9LKyaETLuV44cG9pCpwsnCg`, https://jivo-mark4-astha.vercel.app/.

The live comparison now uses three equal colored bar cards per machine: factory report, saved daily plan and next recommendation. Stopped rows retain the latest non-draft record with a historical label. Target quantities and recorded closed-segment running minutes come from the factory API; no finish time is inferred. Saved and proposed runs retain filling and setup durations separately.

The lower scenario uses its own date selector, independent of Month plan, and explicitly identifies a full-day projection. Its former “shift ahead” wording was misleading at night: the model started at source `meta.as_of` and did not calculate the remaining clock time.

Factory target basis: production-run `required_qty` is cases, per `live/adapters/factory_production.py`; converted using factory `pieces_per_case` and `litres_per_piece`. Time is summed from closed segments' `duration_minutes`, excluding active segments. Invalid or unavailable values remain null. Recorded output can lag operator entry.

Verified before release:
- 120 JavaScript tests; 18 Python collector/publisher tests.
- Typecheck and production build passed on the VPS; Vercel build passed.
- Desktop screenshot: three equal columns, full product names and no overflow.
- Phone 390 × 844: all six machines, stacked bars, document width 390.
- Selecting 9 September changed the scenario only; saved baseline stayed 8 September, captured 19:32:37 IST.
- Selecting Sunday 13 September showed 14 September with an explicit notice; Show Sunday displayed closed 13 September lines.
- Browser-only stale fixture changed all retained statuses and stopped captions to unverified and advice to recheck.
- Browser-only running fixture with absent target and duration displayed unavailable values, without zero filling or ETA.
- Public HTTP 200 and live API fields verified at approximately 23:54 IST.

Original baseline ID remained `2026-09-08:3a0aaca2f2b28f7fcb2d428c7ff9d8c71101226963fcca50f651facebace70ae`.

QA evidence remains on VPS `/root/mark4-astha/bar-layout-qa/`. The deployed source snapshot is `/tmp/mark4-bars-release-20260908/` locally, copied from the tested VPS stage; this avoids including later concurrent work on the separate review page.
