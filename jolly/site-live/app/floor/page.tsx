import FloorClient from "../../components/FloorClient";
export const metadata = {
  title: "Floor map",
  description: "The plant drawn as a floor — machines, oils, the godown and the gate.",
};
export default function Page() { return <FloorClient />; }
