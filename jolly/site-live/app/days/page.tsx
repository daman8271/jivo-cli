import DaysClient from "../../components/DaysClient";
export const metadata = {
  title: "Day by day",
  description:
    "The days already gone, read off the factory's own systems. Today read off the plant, and every day after it " +
    "worked out from today.",
};
export default function Page() { return <DaysClient />; }
