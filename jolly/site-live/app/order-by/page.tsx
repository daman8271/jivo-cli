import OrderByClient from "../../components/OrderByClient";
export const metadata = {
  title: "Order by when",
  description: "Every material, the day its order has to leave, and what stops if it does not.",
};
export default function Page() { return <OrderByClient />; }
