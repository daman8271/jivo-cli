# This repo is PUBLIC (github.com/daman8271/jivo-cli, private:false).
# Everything below is real vendor financial data — balances, payment amounts,
# bank narrations, vendor invoice numbers. It stays on the operator's machine.
# The code in this folder is safe to share; the output of running it is not.
#
# Keep every pattern on its own line. A trailing "# comment" is NOT a comment
# here — it becomes part of the pattern, and the rule silently matches nothing.

site/data.json

# the data is inlined into the page, so the built page is data too
site/index.html

# the kept daily positions
history/

refresh.log
