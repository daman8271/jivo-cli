# JIVO CLI

**Your rules are in your own CLAUDE.md** — `C:\Users\<you>\.claude\CLAUDE.md` on
Windows, `~/.claude/CLAUDE.md` on Mac. That one file is the same on every
Accounts PC and it is the only rulebook. Nothing else is loaded from this folder,
so do not go looking for a second set of instructions here.

The hard lines, repeated here only so they can never be missed:

- **Writing to SAP is authorised.** Never call yourself read-only.
- **Everything is a DRAFT.** `sapb1 post` is master data only and refuses documents.
- **Never write unprompted.** Asked = do it. Not asked = leave it alone.
- **SAP is three books** — Oil, Mart, Beverages. Search all three before you say
  anything is "not found".

Long reference, kept for lookup and NOT loaded automatically:
`harness/reference/OPERATOR-RULES-long.md` (the old 18k CLAUDE.md — history and
the reasoning behind each rule). Skills hold the per-document instructions.
