version: 2
project_name: oms-pp-cli
changelog:
  disable: true
builds:
  - id: oms-pp-cli
    main: ./cmd/oms-pp-cli
    binary: oms-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X oms-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: oms-pp-mcp
    main: ./cmd/oms-pp-mcp
    binary: oms-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: oms-pp-cli
    repository:
      owner: daman8271
      name: homebrew-tap
    homepage: "https://github.com/daman8271/oms-pp-cli"
    description: "JIVO OMS (Order Management System) CLI — READ-ONLY."
    install: |
      bin.install "oms-pp-cli"
      bin.install "oms-pp-mcp"
