# live session data — NEVER commit (JS corpus, probe transcripts, working JSONs)
captures/
*.har
*.session.json
*.jwt

# compiled CLI binary (rebuild: cd cli && go build -o zepto-portal .)
cli/zepto-portal
