# Auth & Identity — read-only probe transcript
# Section slug: auth-identity | lane: platform | hosts: auth-backend.zepto.co.in + fcc.zepto.co.in
# JWT: read from captures/vendor/23-sales-list.txt (authorization header, NO Bearer prefix). Token REDACTED below.
# Token payload: sub 5116e7a0-cc01-4b7d-b098-810cb32dee02, emailId ecom1@jivo.in,
#   roleName "External Super Ads Admin", iat 1783887610, exp 1783967399
#   = exp Mon Jul 13 2026 18:29:59 UTC (23:59:59 IST) -> EXPIRED at probe time (run 2026-07-24, ~11 days lapsed).
# Rules: curl -s -X GET only, read-only allowlist, cap 8 probes, ~1 req/s, STOP on any 401/403/429/WAF.
======================================================

### PROBE 1: GET https://fcc.zepto.co.in/vendor/api/v1/auth/get-user-by-token
# const GET_USER_PROFILE — fetch the current authenticated user's profile from the vendor session layer.
# Chosen because it is the cleanest unambiguous pure-GET read in this section: matches "get"/"user"
#   allowlist words, no path param, no body, no download/export/generate/otp/sign-out/mutate keyword.
#   It resolves the caller's own identity from the bearer JWT — a read.
# Headers: authorization: <jwt-REDACTED>, accept: application/json, origin/referer https://brands.zepto.co.in,
#   normal desktop Chrome User-Agent.
HTTP 401
{"code":401,"message":"Token expired"}

======================================================
HALT: received 401 {"code":401,"message":"Token expired"} on the first probe.
Per READ-ONLY guardrails, stopped immediately after the first 401.
Probes fired: 1.  2xx PROVEN: 0.  All Auth & Identity endpoints remain DOCUMENTED (not probed).

Remaining pure-GET reads left as DOCUMENTED (not probed) — would probe with a fresh token:
  - GET https://fcc.zepto.co.in/api/v1/auth/get-user-by-token   (GET_USER_PROFILE, bare variant; verb to confirm)
  - GET https://auth-backend.zepto.co.in/api/v1/auth/get-user-by-code  (pre-login lookup; needs an invite/reset code)

NOT probed (excluded by the read-only allowlist / method): every sign-in / google-sign-in / verify-invited-user /
  forgot-password-invited / reset-password / resend-code / reinvite-user (auth-backend POST writes), and every
  sign-out / validate-mfa-otp / resend-mfa-otp / remove-user-application-access (fcc session writes — contain
  sign-out / otp / remove keywords). These are DOCUMENTED-FROM-BUNDLE ONLY and held out of scope.

Auth/base note: the SALES/INVENTORY (fcc /api/v1/reports*) and ads (fcc /ads-bff/api/v1) sibling flows use the
  identical `authorization: <jwt>` header (NO Bearer prefix) with origin/referer https://brands.zepto.co.in and
  work when the token is fresh — re-run this probe with a valid token to upgrade get-user-by-token to PROVEN and
  capture its response shape (expected: the user profile object — userId, emailId, roleName, applicationId,
  module/permission list).
