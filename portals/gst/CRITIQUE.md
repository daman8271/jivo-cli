**Verdict: the plan is sound in structure but built on stale inputs — `API.md` (same dir, 2026-08-21) already answers D1, D2, D4, D6, most of D8, and names a 4th host. Fix the inputs before Forge starts, then the rest is mostly small.**

## Problems, by severity

**1. HIGH — Plan missed `/Users/damanpreetsingh/jivo-cli/portals/gst/API.md` and the `HR-*` fixtures.** Finding 1 is wrong on three counts: URLs for 3B/2B/formdetails *are* recorded (API.md §GSTR-3B/2B); Haryana captures with real amounts exist (`fixtures/HR-gstr3b-summary-072026.json` has `itc_elg`, `HR-gstr2b-getdata` has 31 Cr of B2B docs); and `fixtures/` *is* already gitignored (`.gitignore:9`). Also known: login body is `{username,password,captcha,mFP}` (plaintext password, no RSA — your "D1 may reveal encryption" risk is dead), logout is `GET /services/logout`, profile is `POST /services/auth/profile/detail {}`, keepalive is `GET payment/.../api/keepalive`. **Fix:** re-scope Phase 2 to API.md §"Still open" only (D1b/c, D3, D5c, D7, D8a/b/c, D9); build 3B/2B leaves and golden tests now from HR fixtures; drop the Step 0 move.

**2. HIGH — D3 with `curl` measures the wrong thing.** F5 ASM bot-defence keys on TLS/HTTP2 fingerprint as much as on Referer/TS cookies; curl ≠ Go's `crypto/tls`. API.md's "replayed" evidence is `fetch` *from inside Chromium* — still a browser. Nothing yet proves a Go client gets past the WAF, and that is the existential question for the whole build. **Fix:** run D3 as a 30-line `go run` probe (imported browser cookies, UA = the mFP's `Browser.UserAgent`, Referer, Accept) against `ustatus`/`itcbalance`/`cashbalance`, and make it gate Step 5. Do it first, before any other discovery.

**3. HIGH — the login flow will fight the accountant, and cached sessions will rarely survive between runs.** GST portal is (to my recollection, ~80% — verify in D9) single-session-per-user: a CLI login terminates whoever is logged in with that username in a browser, possibly mid-filing. And the idle window is short (~15 min class), so "second `doctor` run uses the cached session" only holds inside that window; in practice every sitting = 8 captchas for `--all`. **Fix:** (a) D9 adds "log in from CLI while a browser session is alive; does the browser die?"; (b) `auth login` prints that warning; (c) make `snapshot` the primary operator verb so one login per GSTIN does everything in one process; (d) don't promise cross-run reuse in docs. Also your D9 5-min curl loop resets the idle timer — it measures absolute lifetime, not idle. Cheaper: read the SPA bundle for its keepalive interval/idle constant, and base64-decode `AuthToken` (if JWT it carries `exp`).

**4. HIGH if tracked, else MED — secrets.** `fixtures/login-xhr-full.json` has a `"password":` key (line 4) — the live Haryana password in plaintext, and `mFP` carries a `deviceID`. `.gitignore` doesn't untrack files committed before the rule. **Run and paste:** `git ls-files portals/gst` and `git log --all --oneline -- 'portals/gst/fixtures/*'`. If anything shows, rotate that GST password (rotation is owed anyway per the env-vault leak). Also: `scrub.py` must redact by key name (`password, captcha, mFP, deviceID, AuthToken, mobNum, email`) not just GSTIN/ARN patterns; AST test forbids `net/http/httputil` (a future `--debug` dump would print the login body); `TestMaskedNeverEchoesPassword` must cover the login *error* path, not just `auth list`.

**5. MED — guard gaps.** (a) 4th host `gstr2b.gst.gov.in` is absent from hosts, session, Referer table, and the literal-regex `^/(services|returns|payment|master)/` (misses `/gstr2b/`, `/services2/`, `/imsweb/`). Invert it: any literal matching `^/[a-z0-9]+/` must be a table path or in a tiny explicit exclusion list. (b) Pinning `http.NewRequest` to two files is weaker than pinning the *import*: only `client.go` may import `net/http`; nothing imports `net`; `os/exec` only in `captcha.go`. Route `login()` through the same `do()` with a `sanctionedPOST` flag. (c) Add `generate|download|gendwnld|offline|summary/generate|save|submit|file|setoff` path substrings to the blocked list so the "generate-then-download" door is a failing test, not a comment. (d) Profile is now a third POST — add it or drop `profile`.

**6. MED — Windows operator reality.** The captcha prompt needs the PNG opened (`cmd /c start "" <tmp>.png` / `open`); headless (SSH, agents) has no display — print the path and say so, and keep `auth import` documented for that case. `TestSessionFileIs0600` must `t.Skip` on windows. `₹` on cmd.exe is mojibake — emit `Rs` or check what dsr-cli does. `captcha-labels/` under the repo dir fills operator sparse clones — put it under `UserConfigDir` with the session files.

**7. MED — scope.** For "compare with SAP later" v0.1 needs: doctor, auth, `returns filed/status/calendar`, `ledger cash/credit`, `gstr3b summary`, `gstr2b summary`, `snapshot` (raw JSON + manifest). Cut `masters`, `returns periods`, `auth whoami`, and **defer `normalise.go`/envelope/golden jsonl** unless the SAP-side comparison spec already has a consumer — that is the biggest chunk of Step 8 and nobody reads it yet. Raw bodies saved per period are enough for Accounts to start.

**8. LOW — lockout.** `maxLoginAttempts = 1` is right, but let the prompt accept `r` to refresh the captcha (free per API.md) before the single POST.

## Forgot

- **ISD registration (07…F2ZX)** files GSTR-6: `filingsnapshot` differs, 3B/2B calls will return non-empty-code errors → `snapshot` must pick the form list from `ustatus.regType/utype`, not hardcode 1/3B.
- **`formdetails?rtn_prd=&rtn_typ=`** (ARN + `fil_dt` per form per period, already replayed) isn't in the command table — it is the single most useful reconciliation read; fold into `returns status`.
- **Politeness/429:** fixed 300–500 ms gap between calls; 429/`Retry-After` → exit 6, never a retry loop.
- **UA consistency:** request `User-Agent` must equal the UA inside the embedded `mFP` blob, or the fingerprint check (if any) trips.
- **Pre-login cookies:** captcha is almost certainly bound to a pre-auth cookie set by `GET /services/login` — same jar for login page → captcha → authenticate, Referer `/services/login` on both.
- Session file is a bearer token to a portal that *can* file returns — say so in SETUP.md.

Confidence: high on 1, 4, 5 (read the files); medium on 2 and 3 (behavioural claims about F5/GST that only D3/D9 can settle).