# live session data / corpus — NEVER commit
captures/js/
captures/probes/
*.har
*.session.json
*.jwt
*.token
.env
# built binary
cli/flipkart-portal
# raw network-capture JSON from walks can carry PII/response bodies — keep local.
# Screenshots + manifests + page text ARE the deliverable evidence (committed).
captures/*-walk/*.json
