# live session data — NEVER commit (JS corpus, probe transcripts, cookie jars, working JSONs)
captures/js/
captures/probes/
.env
*.token
*.har
*.session.json
*.cookies.json
# built binary
cli/amazon-portal
