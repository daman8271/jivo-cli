package main

// Code generated by captures/build_cli.py. DO NOT EDIT BY HAND.
// Section: Ad-Campaigns — see vault/*/Ad-Campaigns.md
// Every command below maps to a READ row in the endpoint inventory;
// the writes and exports of this section are documented in the note and
// are not reachable from here.

import "github.com/spf13/cobra"

func init() { registerSection(newAdCampaignsCmd) }

func newAdCampaignsCmd(app *App) *cobra.Command {
	g := &cobra.Command{Use: "campaigns", Short: "Ad Campaigns (read-only)"}
	g.AddCommand(
		readPost(app, "campaigns", "campaign-suggest-budget", "POST /api/v1/campaign/suggest-budget (SUGGEST_BUDGET)", hostBrandPortal, "/api/v1/campaign/suggest-budget"),
		readPost(app, "campaigns", "campaigns", "POST /api/v1/campaigns (BRAND_INSIGHTS_CAMPAIGNS)", hostBrandPortal, "/api/v1/campaigns"),
		readGet(app, "campaigns", "campaign-bpo", "GET /instamart/v1/campaign/bpo (CAMPAIGN_GET_BPO)", hostPartnerAPI, "/instamart/v1/campaign/bpo"),
	)
	return g
}
