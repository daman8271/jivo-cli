# ARY — Microsoft SQL Server (JOY SERVICES box) — GITIGNORED, do NOT commit.
# Covered by .gitignore *.env. This repo is PUBLIC: verify before any git add:
#   git check-ignore -v connections/ary.env
# Read with:  set -a; . connections/ary.env; set +a
# `sa` is the LOGIN (not a database). Handed over 2026-08-21.
DSR_HOST='138.252.101.118'
DSR_PORT='1433'
DSR_USER='sa'
DSR_PASSWORD='Zero@Jivo'
DSR_DATABASE='master'
DSR_PORTAL_URL=''
