[
  {
    "Text": "select sp.id, sp.persontype, sp.employeeid, sp.personname,   (select min(st.state) from tbl_personstate ps, tbl_states st    where ps.personid=sp.id and ps.stateid=st.stateid) as state,   sp.persongroup, format(daterange.d, 'dd-MM-yyyy') date,   (select min(RIGHT(CONVERT(VARCHAR(23), sp2.timestamp, 121), 12)) from tbl_salespersonattendance sp2    where sp2.personid=sp.id and cast(sp2.timestamp as date)=daterange.d and      sp2.status='P') as start,   (select RIGHT(CONVERT(VARCHAR(23), sp2.timestamp, 121), 12) from tbl_salespersonattendance sp2    where sp2.personid=sp.id and cast(sp2.timestamp as date)=daterange.d and      sp2.status='EOD') as stop,   case     when a.status in ('WO', 'HO', 'CL') then a.status     when a.presentfirsthalf='P' and a.presentsecondhalf ='P' then 'P'     when a.presentfirsthalf='A' and a.presentsecondhalf ='P' then 'Present in second half only'     when a.presentfirsthalf='P' and a.presentsecondhalf ='A' then 'Present in first half only'     when a.presentfirsthalf='A' and a.presentsecondhalf ='A' then 'A'   end as status,   (select count(*) from tbl_salesreport where date=daterange.d and personid=sp.id) count,  (select cast(min(s.timestamp) as Time) from tbl_salesreport s where s.personid = sp.id and status!='BACKEND' and s.date = daterange.d) as firstshop,   (select cast(max(s.timestamp) as Time) from tbl_salesreport s where s.personid = sp.id and status!='BACKEND' and s.date = daterange.d) as lastshop,   (select min(workinghours) from vw_attendance v where v.personid=sp.id and cast(v.timestamp as date)=daterange.d) whours,   a.comment from (   SELECT DATEADD(DAY, t.rownum - 1, '2025-07-01') AS d   FROM tbl_numbertable t   WHERE t.rownum \u003c= DATEDIFF(DAY, '2025-07-01', '2025-07-14') + 1 ) daterange, tbl_attendanceaudit a, tbl_salesperson sp where daterange.d=a.date and sp.deleted=0 and sp.id = 29 and   sp.id=a.personid union select sp.id, sp.persontype, sp.employeeid, sp.personname,   (select min(st.state) from tbl_personstate ps, tbl_states st    where ps.personid=sp.id and ps.stateid=st.stateid) as state,   sp.persongroup, format(daterange.d, 'dd-MM-yyyy'),   (select RIGHT(CONVERT(VARCHAR(23), min(sp2.timestamp), 121), 12) from tbl_salespersonattendance sp2    where sp2.personid=sp.id and cast(sp2.timestamp as date)=daterange.d and      sp2.status='P') as start,   (select RIGHT(CONVERT(VARCHAR(23), max(sp2.timestamp), 121), 12) from tbl_salespersonattendance sp2    where sp2.personid=sp.id and cast(sp2.timestamp as date)=daterange.d and      sp2.status='EOD') as stop,   (select isnull(string_agg(tm,';'),'NA') from (     select distinct concat(status,'/',        case when status='HD' then cast(cast(timestamp as time) as varchar(100)) else '' end     ) tm     from tbl_salespersonattendance sp3     where sp3.personid=sp.id and cast(sp3.timestamp as date)=daterange.d     and status in ('P','WO','A','HD')   ) tmb) as status,   (select count(*) from tbl_salesreport where date=daterange.d and personid=sp.id) count,  (select cast(min(s.timestamp) as Time) from tbl_salesreport s where s.personid = sp.id and status!='BACKEND' and s.date = daterange.d) as firstshop,   (select cast(max(s.timestamp) as Time) from tbl_salesreport s where s.personid = sp.id and status!='BACKEND' and s.date = daterange.d) as lastshop,   (select min(workinghours) from vw_attendance v where v.personid=sp.id and cast(v.timestamp as date)=daterange.d),   null from (   SELECT DATEADD(DAY, t.rownum - 1, '2025-07-01') AS d   FROM tbl_numbertable t   WHERE t.rownum \u003c= DATEDIFF(DAY, '2025-07-01', '2025-07-14') + 1 ) daterange left join tbl_salesperson sp on sp.id = 29 and  1=1 and sp.deleted=0 left join tbl_salespersonattendance a on a.personid=sp.id and cast(a.timestamp as date)=daterange.d where not exists(   select 1 from tbl_attendanceaudit a2   where a2.personid=sp.id and a2.date=daterange.d ) order by 1,6,7"
  },
  {
    "Text": "P nr2 distid=128095 skuID= 326 date=Jul 14 2025  1:08PM . logStock=43 logPrimary= stockFinal=0."
  },
  {
    "Text": "CS - 231197685 - Entry"
  }
]