# credentials — NEVER commit
.env

# nested independent git repos (have their own history)
sap-b1/
control-panel/

# OS noise
.DS_Store

# nested independent repo
postsql/
.gstack/

# tunnel/share access files
*.env
.env*
*.local.md

# live session tokens — NEVER commit
*.token
.token
token.json
session.json
